[project]
name = "uv-hero"
version = "0.1.0"
description = "Hello-world CLI built during the uv workshop."
requires-python = ">=3.13"
dependencies = [
    "click>=8.1",
]

[project.scripts]
uv-hero = "uv_hero.cli:hello"

[build-system]
requires = ["uv_build>=0.5"]
build-backend = "uv_build"
